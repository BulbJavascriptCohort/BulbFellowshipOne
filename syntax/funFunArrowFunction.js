function multiplication(h, j) {
    return h * j
}

// variable function declaration
const a = 2

const mult = function (h, j) {
    return h * j
}

console.log(multiplication(9, 8));
console.log(mult(9, 8));

// arrow function, lambda

const multi = (h, j) => {
    return h * j
}

let mul = (h, j) => h * j

console.log(multi(9, 8));
